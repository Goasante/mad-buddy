import type { SubscriptionPlan } from "@/lib/supabase/database.types";

/** Retired consumer status badge. Mad Buddy Access is not a public identity. */
export function PremiumPlanBadge(_props: { plan: SubscriptionPlan | null | undefined; compact?: boolean; className?: string }) {
  return null;
}
