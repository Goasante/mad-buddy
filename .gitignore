node_modules
.next
out
dist
build
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo
.vercel
.dev-server.log
.dev-server.err.log
.dev-server.pid
tmp/

# Firebase Android config — contains an API key. Keep it local; each dev/CI
# supplies their own. Remove this line if you deliberately commit it (private repo).
android/app/google-services.json
